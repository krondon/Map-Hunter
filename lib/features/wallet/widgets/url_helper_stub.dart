/// Stub implementation for non-web platforms.
/// On native platforms (iOS app, Android, Windows) url_launcher handles this.
void openUrlInNewTab(String url) {
  // No-op: handled by url_launcher on native
}
